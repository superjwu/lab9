# Lab 9

This lab starts one registrar and three nodes with Docker.
The nodes register, connect to peers, share transactions,
and mine blocks on the same network.

The container was not pushed to scp, but is included in this git repo.
## Running

```sh
docker compose up --build
```

Stop it with `docker compose down`.


